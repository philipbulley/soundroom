describe('authReducer', () => {
	xit('should call with loadUserAction', () => {
		// TODO: Use rewire to mock reducer commands
	});

	xit('should call with loadUserErrorAction', () => {
		// TODO: Use rewire to mock reducer commands
	});

	xit('should call with loadUserSuccessAction', () => {
		// TODO: Use rewire to mock reducer commands
	});
});
