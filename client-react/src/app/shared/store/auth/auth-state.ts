export enum AuthStatus {
  // tslint:disable:no-any
  LOADING = `AuthState.LOADING` as any as AuthStatus,
  LOGGED_IN = `AuthState.LOGGED_IN` as any as AuthStatus,
  LOGGED_OUT = `AuthState.LOGGED_OUT` as any as AuthStatus,
  // tslint:enable:no-any
}
